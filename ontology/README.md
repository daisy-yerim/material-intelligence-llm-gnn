# Ontology

This folder is reserved for ontology-related extensions.

The current graph schema already defines practical entities and relations such as messages, materials, products, organizations, clusters, and relation types.  
A future ontology version can formalize these concepts with OWL/RDF and validation rules.
